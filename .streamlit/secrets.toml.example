# Copy this file to .streamlit/secrets.toml (which is git-ignored) and fill in values.
# The app reads GEMINI_API_KEY from here so you don't have to paste it every session.

GEMINI_API_KEY = "your-gemini-api-key"
